apiVersion: v1
kind: Service
metadata:
  name: risingwavepoc-app
  namespace: {{ .Values.namespace }}
spec:
  type: NodePort
  selector:
    app: risingwavepoc-app
  ports:
    - port: {{ .Values.app.port }}
      targetPort: {{ .Values.app.port }}
      nodePort: {{ .Values.app.nodePort }}
